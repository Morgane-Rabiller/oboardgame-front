const initialState = {
    user: {
        logged: false,
        email: "",
        pseudo: "",
        password: ""
    }
};

export default function userReducer(state = initialState, action) {
    // switch (action.type) {
    //     default: return state;
    // }
    return state;
}