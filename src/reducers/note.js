const initialState = {
    note: "",
    hasNote: false,
};

export default function noteReducer(state = initialState, action) {
    switch (action.type) {
      default:
        return state;
    }
}